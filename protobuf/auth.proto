syntax = "proto3";

option go_package = "protobuf";

package auth;

message Request {
  string username = 1;
  string password = 2;
}

message Response {
  string result = 1;
}

service AUTH {
  rpc AuthLogin (stream Request) returns (stream Response) {}
}
